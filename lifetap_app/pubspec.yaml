name: lifetap_app
description: "LifeTap - educational emergency-assistance prototype (student project)."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # Added in later stages:
  # http: ^1.2.0            (Stage 5B - API calls)
  # shared_preferences: ^2.2.0  (Stage 5B - token storage)
  # geolocator: ^12.0.0     (Stage 5E - GPS)
  # speech_to_text: ^7.0.0  (Stage 5F - voice input)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
